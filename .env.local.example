NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Optional — only needed for the AI Security Assistant tool (/tools/ai-assistant).
# Get a free key at https://aistudio.google.com/apikey. Without this set, the
# rest of the site works normally; that one tool just shows a friendly
# "belum dikonfigurasi" message instead of crashing.
GEMINI_API_KEY=your-gemini-api-key
